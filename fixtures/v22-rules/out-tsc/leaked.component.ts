import { Component } from '@angular/core';

/** Build output. A compiled library re-emits every component here. */
@Component({ selector: 'app-leaked', template: `<p>built</p>` })
export class LeakedComponent {}
