import { Component } from '@angular/core';

/** A test file. Declares a component, and must never be counted as one. */
@Component({ selector: 'app-spec-only', template: `<p>spec</p>` })
export class SpecOnlyComponent {}
